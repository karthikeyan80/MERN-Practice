/* function Car(make, model, year, colour) {
  this.make = make;
  this.model = model;
  this.year = year;
  this.colour = colour;
  this.start = function () {
    console.log(`Starting the ${this.make} ${this.model}`);
  };
  this.stop = function () {
    return `Stopping the ${this.make} ${this.model}`;
  };
}

const car1 = new Car("BMW", 1, 2020, "red");
const car2 = new Car("AUDI", 2, 2020, "blue");


car1.start();
a=car2.stop();
console.log(a);

console.log(typeof car1); */


// --------------------------------------------------------

/* 
class Hero {
  constructor(name, level) {
    this.name = name;
    this.level = level;
  }
  greet() {
    return `he is ${this.name}`;
  }
}

class Mega extends Hero {
  constructor(name, level, spell) {
    super(name, level);
    this.spell = spell;
  }
}

const meg = new Mega("SpiderMan", 100, "web");
console.log(meg);
console.log(meg.name);
console.log(meg.greet());
 */



